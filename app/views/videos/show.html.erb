<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @video.name %>
</p>

<p>
  <strong>Section:</strong>
  <%= @video.section_id %>
</p>

<%= link_to 'Edit', edit_course_section_video_path(@course, @section, @video) %> |
<%= link_to 'Back', course_section_path(@section.course_id, @section.id) %>
